# Critical Alert Lane

Live: https://critical-alert-lane.sociobot.in — built by the Param Factory (`android`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
